import React from 'react';

import './Node.css';

type NodeProps = {
    color: string
};

const Node = ({color}: NodeProps) => {
    return(
        <div className="node">

        </div>
    );
};

export default Node;